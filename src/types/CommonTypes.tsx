export type jsonNode = {
        id?: string;
        title?: string;
        description?: string;
        link?: string;
        image?: string;
    }
export type projectNode = {
    node: jsonNode;
}
